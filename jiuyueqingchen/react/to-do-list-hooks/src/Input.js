import React from 'react';

export default function Inputc() {
    return (
        <input
            className="new-list"
            type="text"
            placeholder="What needs to be done?"
            autoComplete="off"
        />
    );
}